<!DOCTYPE html>
<html lang="en">

@await Html.PartialAsync("/Views/Default/PartialHead.cshtml")
<body>
<div id="preloder">
    <div class="loader"></div>
</div>

@await Html.PartialAsync("/Views/Default/Topbar.cshtml")

@await Html.PartialAsync("/Views/Default/Header.cshtml")
@RenderBody()
<div class="custom-notification" id="customNotification">Product added to cart!</div>
<div class="custom-notification" id="customWishlistNotification">Product added to favorites!</div>

@await Html.PartialAsync("/Views/Default/Footer.cshtml")
<div class="search-model">
    <div class="h-100 d-flex align-items-center justify-content-center">
        <div class="search-close-switch">+</div>
        <form class="search-model-form">
            <input type="text" id="search-input" placeholder="Search here.....">
        </form>
    </div>
</div>

@await Html.PartialAsync("/Views/Default/ScriptPartial.cshtml")
</body>
@RenderSection("scripts", required: false)
</html>